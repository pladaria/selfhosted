{
    "name": "mdocs",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "dev": "concurrently --names \"frontend,server\" --prefix-colors \"cyan,yellow\" --kill-others \"npm run dev --prefix frontend\" \"npm run dev --prefix server\"",
        "dev:frontend": "npm run dev --prefix frontend",
        "dev:server": "npm run dev --prefix server",
        "install:all": "npm install --prefix frontend && npm install --prefix server",
        "build": "npm run build --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
